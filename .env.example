DATABASE_URL="postgresql://user:pass@localhost:5432/pierre?schema=public"
# Conexao direta, sem pooler: usada so pelo `prisma migrate`.
# Em desenvolvimento e igual a DATABASE_URL.
DIRECT_URL="postgresql://user:pass@localhost:5432/pierre?schema=public"

JWT_SECRET="troque-por-32-bytes-aleatorios"

# Assistente Bean.counter — opcional. Sem chave, o assistente roda no motor de regras local.
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-opus-5"

# Open Finance — agregador (Pluggy / Belvo / similar). Sem chaves, o app usa o
# adaptador sandbox que gera dados fake para desenvolvimento.
OPEN_FINANCE_PROVIDER="sandbox"
OPEN_FINANCE_CLIENT_ID=""
OPEN_FINANCE_CLIENT_SECRET=""
OPEN_FINANCE_BASE_URL=""

# Captura rápida por Telegram — opcional.
# Crie o bot com @BotFather, e registre o webhook apontando para
# https://SEU_ENDERECO/api/telegram/webhook?segredo=SEU_SEGREDO
TELEGRAM_BOT_TOKEN=""
TELEGRAM_WEBHOOK_SEGREDO=""
